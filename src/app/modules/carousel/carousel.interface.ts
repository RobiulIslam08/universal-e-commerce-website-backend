export interface ICarousel {
  title: string;
  subtitle: string;
  badge: string;
  badgeSubtext: string;
  bgColor: string;
  image?: string;
  buttonText?: string;
  buttonLink?: string;
  isActive: boolean;
  order: number;
}
